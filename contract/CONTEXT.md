# Location: `ProjectX/contract/`

Scope: Immediate contents only (non-recursive).

This folder contains:

- `__init__.py`: package-level description for the contract package.
- `CONTEXT.md`: folder-level inventory and package boundary note.
- `CONTEXTCHECK.md`: most recent folder context audit report.
- `RECURSIVECONTEXTCHECK.md`: recursive context audit report for `contract/`.
- `shared/`: shared primitives package.
- `worker/`: worker-specific contract implementation package.
- `gateway/`: gateway-specific contract implementation package.
- `__pycache__/`: Python bytecode cache artifacts for this package root.

The `contract` folder acts as the package root that organizes these subpackages.

Boundary definition reference: `ProjectX/BOUNDARY_MATRIX.md`

